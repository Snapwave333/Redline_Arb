# ARBYS Environment Configuration Template (.env)
# Copy this file to .env and fill in your actual values.
# The Desktop Client automatically loads .env via python-dotenv.
# The Backend Flask API also loads .env (backend/api_server.py).

# -----------------------------------------------------------------------------
# WINDOWS POWERSHELL QUICK TIP (optional)
# -----------------------------------------------------------------------------
# You can also set environment variables in your current PowerShell session:
#   $env:API_SPORTS_KEY = "<your api-sports key>"
#   $env:THE_ODDS_API_KEY = "<your the-odds-api key>"
#   $env:ARBYS_BACKEND_URL = "http://localhost:5000"
# These settings last for the duration of the PowerShell session.

# -----------------------------------------------------------------------------
# BACKEND SERVER CONFIG
# -----------------------------------------------------------------------------
# If you run the Desktop Client and Backend together locally,
# point the GUI to the backend health/opportunities endpoints here:
ARBYS_BACKEND_URL=http://localhost:5000

# Flask server bind settings (optional)
HOST=127.0.0.1
PORT=5000
DEBUG=0

# -----------------------------------------------------------------------------
# PROVIDER KEYS (REAL DATA ONLY)
# -----------------------------------------------------------------------------
# SofaScore Scraper (free; undocumented; use responsibly)
SOFASCORE_ENABLED=1

# API-SPORTS (free plan ~100 requests/day)
# Either of these will be recognized; prefer API_SPORTS_KEY
API_SPORTS_KEY=your_api_sports_key_here
APISPORTS_API_KEY=

# The Odds API (optional/paid)
# Either of these will be recognized; prefer THE_ODDS_API_KEY
THE_ODDS_API_KEY=your_the_odds_api_key_here
ODDS_API_KEY=

# -----------------------------------------------------------------------------
# ARBITRAGE / UI SETTINGS
# -----------------------------------------------------------------------------
MIN_PROFIT_PERCENTAGE=1.0
MAX_STAKE=1000.0
DEFAULT_STAKE=100.0

# Alert Configuration
ENABLE_SOUND_ALERTS=false
PROFIT_THRESHOLD_ALERT=5.0

# Anti-Detection Settings
ROUND_STAKES=true
VARY_BET_SIZES=true
MAX_BET_VARIATION_PERCENT=5.0

# Update Interval (seconds) for GUI auto-refresh
UPDATE_INTERVAL=30

# Data Policy (security)
# Enforce real data only (disables any synthetic/top-up generation paths)
REAL_DATA_ONLY=true
# Synthetic opportunities (ignored when REAL_DATA_ONLY=true)
SYNTHESIZE_OPPORTUNITIES_WHEN_LOW=false


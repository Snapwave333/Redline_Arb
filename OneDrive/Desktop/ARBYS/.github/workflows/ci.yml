name: CI

on:
  push:
    branches: [main, master, develop]
    tags: ["v*"]
  pull_request:
    branches: [main, master, develop]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11"]
    
    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Install uv
        uses: astral-sh/setup-uv@v1
        with:
          version: "latest"
      
      - name: Install dependencies
        run: |
          uv pip install --upgrade pip
          uv pip install -U -r requirements.txt
          uv pip install nox
      - name: Record Python version inventory
        run: |
          mkdir -p version_inventory
          python -c "import platform,sys; print('OS:', platform.platform()); print('Python:', sys.version)" | tee version_inventory/python_env.txt
          python -m pip --version | tee version_inventory/pip_version.txt
          python -m pip freeze | tee version_inventory/python_freeze.txt
          python -m pip list --outdated | tee version_inventory/python_outdated.txt
      - name: Fail if outdated Python packages remain
        shell: bash
        run: |
          if grep -qE "^Package" version_inventory/python_outdated.txt; then
            echo "Outdated Python packages remain after update:" && cat version_inventory/python_outdated.txt
            exit 1
          else
            echo "Python dependencies up-to-date."
          fi
      
      - name: Run linting
        run: nox -s lint
        continue-on-error: false
      
      - name: Run type checking
        run: nox -s typecheck
        continue-on-error: false
      
      - name: Run tests with coverage
        run: nox -s test
        continue-on-error: false
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
        continue-on-error: true
      
      - name: Upload benchmark artifacts
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ matrix.os }}-py${{ matrix.python-version }}
          path: .benchmarks/
          retention-days: 7
      - name: Upload backend version inventory
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-version-inventory-${{ matrix.os }}-py${{ matrix.python-version }}
          path: version_inventory/**
          retention-days: 14
      
      - name: Run security scan
        run: nox -s sec
        continue-on-error: true
      
      - name: Run hygiene checks
        run: nox -s hygiene
        continue-on-error: true

  frontend-qa:
    # Temporarily disabled per production focus on desktop client and backend only
    # To re-enable mobile web app QA, remove or set this condition to true.
    if: false
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [18, 20, 22, 24]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'
          cache-dependency-path: mobile_web_app/package-lock.json

      - name: Install dependencies
        working-directory: mobile_web_app
        run: npm ci
      - name: Update Node dependencies to latest within ranges
        working-directory: mobile_web_app
        run: npm update --save
      - name: Record Node version inventory
        working-directory: mobile_web_app
        run: |
          mkdir -p version_inventory
          node -v | tee version_inventory/node_version.txt
          npm -v | tee version_inventory/npm_version.txt
          npm ls --depth=0 --json > version_inventory/node_deps.json
      - name: Fail if outdated Node packages remain
        working-directory: mobile_web_app
        run: |
          npm outdated

      - name: Install Playwright Browsers
        working-directory: mobile_web_app
        run: npx playwright install --with-deps

      - name: Run smoke tests (quality gate)
        working-directory: mobile_web_app
        run: npm run test:smoke

      - name: Run accessibility tests
        working-directory: mobile_web_app
        run: npx playwright test tests/e2e/07_accessibility.spec.ts --reporter=line

      - name: Run performance tests
        working-directory: mobile_web_app
        run: npx playwright test tests/e2e/09_perf.spec.ts --reporter=line

      - name: Run full E2E test suite
        working-directory: mobile_web_app
        run: npm run test:e2e

      - name: Upload test reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-node${{ matrix.node }}
          path: |
            mobile_web_app/playwright-report/
            mobile_web_app/test-results/
            mobile_web_app/reports/
            mobile_web_app/version_inventory/
          retention-days: 30

  packaging:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        include:
          - os: windows-latest
            name: windows
            script: build_windows.bat
            artifact_suffix: Windows_x64.exe
          - os: macos-latest
            name: macos
            script: build_macos.sh
            artifact_suffix: macOS_universal.dmg

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -U -r requirements.txt
          pip install pyinstaller==6.16.0
      - name: Record Python version inventory (packaging)
        run: |
          mkdir -p version_inventory
          python -c "import platform,sys; print('OS:', platform.platform()); print('Python:', sys.version)" | tee version_inventory/python_env.txt
          python -m pip --version | tee version_inventory/pip_version.txt
          python -m pip freeze | tee version_inventory/python_freeze.txt
          python -m pip list --outdated | tee version_inventory/python_outdated.txt
      - name: Fail if outdated Python packages remain (packaging)
        shell: bash
        run: |
          if grep -qE "^Package" version_inventory/python_outdated.txt; then
            echo "Outdated Python packages remain after update:" && cat version_inventory/python_outdated.txt
            exit 1
          else
            echo "Python dependencies up-to-date."
          fi

      - name: Create macOS icon (macOS only)
        if: matrix.os == 'macos-latest'
        run: |
          chmod +x scripts/create_macos_icon.sh
          ./scripts/create_macos_icon.sh

      - name: Create iPad icons (macOS only)
        if: matrix.os == 'macos-latest'
        run: |
          chmod +x scripts/create_ipad_icons.sh
          ./scripts/create_ipad_icons.sh

      - name: Build Windows package (Python 3.10)
        if: matrix.os == 'windows-latest'
        run: |
          scripts\\build_windows.bat
        env:
          VERSION_OVERRIDE: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}

      - name: Build macOS package (Python 3.10)
        if: matrix.os == 'macos-latest'
        run: |
          chmod +x scripts/build_macos.sh
          VERSION_OVERRIDE=${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }} ./scripts/build_macos.sh

      - name: Generate checksums (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          chmod +x scripts/generate_checksums.sh
          ./scripts/generate_checksums.sh dist

      - name: Generate checksums (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $outFile = Join-Path "dist" "checksums.txt"
          "# SHA256 Checksums for Redline Arbitrage Release" | Out-File -FilePath $outFile -Encoding utf8
          "# Generated on $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')" | Out-File -FilePath $outFile -Append -Encoding utf8
          "" | Out-File -FilePath $outFile -Append -Encoding utf8
          Get-ChildItem -Path dist -File -Include *.exe, *.zip | Sort-Object Name | ForEach-Object {
            $hash = (Get-FileHash -Algorithm SHA256 $_.FullName).Hash
            "SHA256 ($($_.Name)) = $hash" | Out-File -FilePath $outFile -Append -Encoding utf8
          }
          "" | Out-File -FilePath $outFile -Append -Encoding utf8
          "# Verify with: Get-FileHash -Algorithm SHA256 <file>" | Out-File -FilePath $outFile -Append -Encoding utf8

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}-build
          path: |
            dist/**/*
          retention-days: 7
        continue-on-error: false
      - name: Upload packaging version inventory
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: packaging-version-inventory-${{ matrix.name }}
          path: version_inventory/**
          retention-days: 14

  release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/')
    needs: [packaging]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: artifacts/windows

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-build
          path: artifacts/macos

      - name: List assets
        run: |
          ls -la
          echo "Windows assets:" && ls -la artifacts/windows || true
          echo "macOS assets:" && ls -la artifacts/macos || true

      - name: Select release notes path
        shell: bash
        run: |
          if [ -f "RELEASE_NOTES_${GITHUB_REF_NAME}.md" ]; then
            echo "BODY_PATH=RELEASE_NOTES_${GITHUB_REF_NAME}.md" >> $GITHUB_ENV
          else
            echo "BODY_PATH=RELEASE_NOTES_v1.0.0.md" >> $GITHUB_ENV
          fi

      - name: Create GitHub Release and Upload Assets
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          prerelease: false
          body_path: ${{ env.BODY_PATH }}
          files: |
            artifacts/windows/**
            artifacts/macos/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
